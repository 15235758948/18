<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="CustomMenuStyle" parent="OptionMenuStyle">
        <item name="android:padding">8dp</item>
        <item name="android:textColor">@color/colorAccent</item>
        <item name="android:background">@drawable/bg_custom_menu</item>
    </style>
    <style name="AppTheme.Button.18master" parent="Widget.AppCompat.Button.Colored">
        <item name="colorButtonNormal">@color/colorAccent</item>
        <item name="android:textColor">@color/colorBai</item>
        <item name="android:textSize">@dimen/size_20dp</item>
    </style>
    <style name="AppTheme.EditText.Big" parent="Widget.AppCompat.EditText">
        <item name="android:drawablePadding">6dp</item>
        <item name="android:paddingLeft">6dp</item>
        <item name="android:paddingRight">6dp</item>
        <item name="android:textSize">20dp</item>
        <item name="android:textColorHint">@color/text_hint</item>
        <item name="android:textColor">@color/text_goods_name</item>
        <item name="android:singleLine">true</item>
        <item name="android:background">@drawable/edit_bg_long</item>
    </style>
    <style name="popwin_anim_style">
    <item name="android:windowEnterAnimation">@anim/style</item>
    <item name="android:windowExitAnimation">@anim/style</item>
    </style>
</resources>
